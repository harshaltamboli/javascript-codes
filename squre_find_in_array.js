// Print the squares of all numbers in the array [1, 2, 3, 4, 5].
// Hint: Multiply each element with itself.


let a=[1,2,3,4,5];
let empty=[];
a.forEach((current_value,index,array) =>{

// empty.push(current_value*current_value);

console.log(`${current_value} = ${current_value * current_value}`);

});
// console.log(empty);

